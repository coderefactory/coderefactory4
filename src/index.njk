---
layout: base
eleventyNavigation:
  key: Home
---

<section id="home" data-anchor="home">
  <div class="intro__layout container">
    <h1>{{ build.orgName }}</h1>
    <h2>
      <span>
        Code Refactory is a small collective of 
        <br/>
        web software developers who emphasize 
        <br/>
        craftsmanship and close collaboration 
        <br/>
        in delivering high-value software.
      </span>
      <span>This is how we solve problems.</span>
      <span>{% icon "icon-down-arrow" %}</span>
    </h2>
  </div>
</section>

<section id="work" data-anchor="work">
  <div class="stories__layout">
    {% for story in stories %}
      <style>
        #work .block--story.story{{ loop.index }}:before {
          background-image: linear-gradient(rgba(0, 8, 27, 0.87) 0%, transparent 85%), url(/assets/images/stories/{{ story.imageDir }}/hero.jpg);
        }
      </style>
      <div class="block--story story{{ loop.index }}">
        <div class="story-content">
          <div class="story-content__intro">
            <h5>{{ story.client }}</h5>
            <h2>{{ story.summary | safe }}</h2>
            <p>{{ story.narrative }}</p>
          </div>
          <div class="story-content__carousel">
            <button class="open-story">Learn more</button>
            <button class="close-story">&times;</button>
            <div class="story-content__slides">
              <ul>
                {% for slide in story.slides %}
                  <li>
                    <div class="story-slide__image">
                      <img src="/assets/images/stories/{{ story.imageDir }}/{{ slide.filename }}" />
                    </div>
                    <div class="story-slide__text">
                      {% for slideText in slide.grafs %}
                        <p>{{ slideText | safe }}</p>
                      {% endfor %}
                    </div>
                  </li>
                {% endfor %}
              </ul>
              <button class="slide-nav prev-slide" disabled="disabled">{% icon "icon-down-arrow" %}</button>
              <button class="slide-nav next-slide">{% icon "icon-down-arrow" %}</button>
              <div class="progress-indicator" data-count="{{ story.slides | length }}">
                <span>slide 1</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    {% endfor %}
  </div>
</section>

<section id="experience" data-anchor="experience">
  <div class="experience__layout container">
    <div class="block--clients">
      <h2 class="h3">We've built things for</h2>
      <ul>
        {% for clientLogo in experience.clients %}
          <li>
            {% include "../src/assets/images/logos/" + clientLogo.filename + ".svg" %}
            <span class="sr-only">{{ clientLogo.title }}</span>
          </li>
        {% endfor %}
      </ul>
    </div>
    <div class="block--tools">
      <h2 class="h3">Using tools like</h2>
      <ul>
        {% for toolLogo in experience.tools %}
          <li>
            {% include "../src/assets/images/logos/" + toolLogo.filename + ".svg" %}
            <span class="sr-only">{{ toolLogo.title }}</span>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</section>

<section id="about" data-anchor="about">
  <div class="about__layout container">
    <h2 class="h3">Experienced, adaptable, local. <br />Meet your team.</h2>
    <div class="staff__layout">
      {% for staff in about %}
        <div class="block--staff">
          <img src="/assets/images/about/{{ staff.headshot }}" alt="{{ staff.name }}" />
          <h4>{{ staff.name }}</h4>
          <p>{{ staff.bio }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<section id="contact" data-anchor="contact">
  <div class="contact__layout container">
    <h2>Say hey.</h2>
    <h3>Got an ambitious project in mind?<br />Let's talk.</h3>
    <p>
      We can help bring your ideas to market expertly and quickly. 
      <br />
      Tell us about your project, and how we can reach you.
    </p>
    <!-- 
    <p>
      We can help bring your ideas to market expertly and quickly. 
      <br />
      Tell us about your project, and how we can reach you.
    </p>
    <p>
      We can help bring your ideas to market expertly and quickly. 
      <br />
      Tell us about your project, and how we can reach you.
    </p>
    <p>
      We can help bring your ideas to market expertly and quickly. 
      <br />
      Tell us about your project, and how we can reach you.
    </p>
    -->
    <!-- 
    <p class="contact">
      <a href="mailto:info@coderefactory.com">
        <span class="envelope">{% icon "icon-envelope" %}</span>
        <span>info@coderefactory.com</span>
      </a>
    </p>
    -->
    <form id="contact-form">
      <label id="contact-form__label" for="contact-form__message">
        <span id="contact-form__input" contenteditable="true" role="textbox" aria-labelledby="contact-form__label-text"><span contenteditable="false"></span></span>
        <span id="contact-form__label-text">Type here</span>
      </label>
      <input type="hidden" name="message" id="contact-form__message" />
      <button type="submit">Send</button>
    </form>
  </div>
  {% include "footer.njk" %}
</section>
